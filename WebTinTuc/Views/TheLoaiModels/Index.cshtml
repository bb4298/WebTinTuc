@model IEnumerable<WebTinTuc.Models.TheLoaiModels>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenTheLoai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MoTa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenTheLoai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoTa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TheLoaiID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TheLoaiID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TheLoaiID })
        </td>
    </tr>
}

</table>
